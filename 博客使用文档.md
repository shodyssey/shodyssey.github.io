# Hexo + Butterfly 博客使用文档

> 博客地址：https://zhangshihang2.github.io
> 本地项目：`~/github/my-blog/`
> 技术栈：Hexo + Butterfly 主题 + GitHub Pages

---

## 一、日常命令速查

| 操作 | 命令 |
|------|------|
| 新建文章 | `hexo new "文章标题"` |
| 本地预览 | `hexo server`（→ http://localhost:4000） |
| 生成静态文件 | `hexo generate` |
| 部署到 GitHub | `hexo deploy` |
| 一键发布 | `hexo clean && hexo generate && hexo deploy` |
| 清除缓存 | `hexo clean` |

---

## 二、UI 界面自定义

所有 UI 配置在 `_config.yml`（站点配置）和 `_config.butterfly.yml`（主题配置）中修改。

### 2.1 创建主题配置文件

```bash
# 将主题默认配置复制为可覆盖的配置文件
cp node_modules/hexo-theme-butterfly/_config.yml _config.butterfly.yml
```

> 之后所有 UI 修改都在 `_config.butterfly.yml` 中进行，不要直接改 `node_modules` 里的文件。

### 2.2 站点基本信息（`_config.yml`）

```yaml
# Site
title: ZhangShihang's Blog          # 博客标题
subtitle: '技术学习与思考'            # 副标题
description: '分享推荐系统的学习记录'   # 站点描述（SEO）
keywords: 推荐系统,机器学习            # 关键词
author: ZhangShihang2                # 作者名
language: zh-CN                      # 语言
timezone: Asia/Shanghai              # 时区
```

### 2.3 导航栏菜单（`_config.butterfly.yml`）

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  分类: /categories/ || fas fa-folder-open
  标签: /tags/ || fas fa-tags
  关于: /about/ || fas fa-user
```

> 格式：`名称: 路径 || 图标类名`，图标参考 [Font Awesome](https://fontawesome.com/icons)

### 2.4 头像和 Favicon

```yaml
# _config.butterfly.yml
favicon: /img/favicon.png            # 浏览器标签页图标
avatar:
  img: /img/avatar.jpg               # 侧边栏头像
  effect: false                       # 是否开启头像旋转动效
```

> 图片放在 `source/img/` 目录下。

### 2.5 社交链接

```yaml
# _config.butterfly.yml
social:
  fab fa-github: https://github.com/ZhangShihang2 || Github || '#24292e'
  fas fa-envelope: mailto:your@email.com || Email || '#4a7dbe'
```

### 2.6 侧边栏设置

```yaml
# _config.butterfly.yml
aside:
  enable: true
  position: right                     # 侧边栏位置：left / right
  display:
    archive: true                     # 显示归档
    tag: true                         # 显示标签
    category: true                    # 显示分类
  card_author:
    enable: true
    description: '分享学习记录与思考'   # 个人描述
    button:
      enable: true
      icon: fab fa-github
      text: Follow Me
      link: https://github.com/ZhangShihang2
  card_announcement:
    enable: true
    content: '分享学习记录 & 思考过程'  # 公告内容
  card_recent_post:
    enable: true
    limit: 5                          # 显示最近文章数量
```

### 2.7 页脚设置

```yaml
# _config.butterfly.yml
footer:
  owner:
    enable: true
    since: 2025                        # 建站年份
  copyright:
    enable: true
  custom_text: '用 Hexo + Butterfly 搭建'  # 自定义页脚文字
```

### 2.8 首页布局

```yaml
# _config.butterfly.yml
# 首页文章布局：1-7 可选
# 1: 封面在左，信息在右
# 2: 封面在右，信息在左
# 3: 封面左右交替（默认）
# 4: 封面在上，信息在下
index_layout: 3

# 首页文章摘要
index_post_content:
  method: 3                           # 3=自动摘要（默认）
  length: 500                         # 摘要字数
```

### 2.9 文章封面图

```yaml
# _config.butterfly.yml
cover:
  index_enable: true                  # 首页显示封面
  aside_enable: true                  # 侧边栏显示封面
  archives_enable: true               # 归档页显示封面
  default_cover:                      # 默认封面（无封面时使用）
    - /img/default-cover-1.jpg
    - /img/default-cover-2.jpg
```

### 2.10 代码块样式

```yaml
# _config.butterfly.yml
code_blocks:
  theme: light                        # 主题：darker / pale night / light / ocean
  macStyle: false                     # Mac 风格代码块
  copy: true                          # 显示复制按钮
  language: true                      # 显示语言标签
  shrink: false                       # 默认折叠/展开
```

### 2.11 文章版权声明

```yaml
# _config.butterfly.yml
post_copyright:
  enable: true                        # 开启文章版权声明
  license: CC BY-NC-SA 4.0            # 许可协议
  license_url: https://creativecommons.org/licenses/by-nc-sa/4.0/
```

### 2.12 网站背景

```yaml
# _config.butterfly.yml
# 纯色背景
background: '#f5f5f5'

# 图片背景
background: https://example.com/bg.jpg

# 随机背景（数组中随机选择）
background:
  - https://example.com/bg1.jpg
  - https://example.com/bg2.jpg
```

---

## 三、文章管理

### 3.1 创建新文章

```bash
cd ~/github/my-blog
hexo new "Weighted LR 调查报告"
```

生成的文件：`source/_posts/Weighted-LR-调查报告.md`

### 3.2 文章 Front-matter（头部元信息）

```markdown
---
title: Weighted LR 调查报告
date: 2026-08-31 20:00:00
categories:
  - 推荐系统
  - 机器学习
tags:
  - Weighted LR
  - YouTube
  - 时长建模
cover: /img/cover.jpg
description: 'YouTube 推荐系统中 Weighted LR 的原理与演进'
---
```

| 字段 | 说明 | 示例 |
|------|------|------|
| `title` | 文章标题 | `Weighted LR 调查报告` |
| `date` | 发布日期 | `2026-08-31 20:00:00` |
| `categories` | 分类（支持多级） | `推荐系统 / 机器学习` |
| `tags` | 标签 | `Weighted LR, YouTube` |
| `cover` | 封面图路径 | `/img/cover.jpg` |
| `description` | 摘要描述 | 文章简介 |
| `top` | 置顶 | `true` 或数字（越大越靠前） |

### 3.3 修改文章内容

```bash
# 1. 编辑 source/_posts/ 下的 md 文件（用任意编辑器）
#    例如编辑 weighted-lr-research.md

# 2. 修改完成后重新部署
cd %USERPROFILE%\github\my-blog
hexo clean && hexo generate && hexo deploy
```

> 修改 Front-matter 中的 `date` 会影响文章排序，如需更新排序可在 Front-matter 中加 `updated: 2026-09-01 10:00:00`

### 3.4 修改文章分类/标签

编辑文章的 Front-matter 部分：

```yaml
---
title: Weighted LR 调查报告
date: 2026-08-31 21:00:00
categories:
  - 推荐系统          # 改分类
  - 机器学习          # 可加多级
tags:
  - Weighted LR       # 改标签
  - YouTube
---
```

改完后同样执行：
```bash
hexo clean && hexo generate && hexo deploy
```

### 3.5 文章下线

#### 方式一：转为草稿（推荐，保留内容，随时恢复）

```bash
# 1. 将文章从 _posts 移到 _drafts
move "source\_posts\文章名.md" "source\_drafts\"

操作	Windows CMD	Git Bash (MINGW64)
移动文件	move	mv
删除文件	del	rm
复制文件	copy	cp
路径分隔符	\	/


# 2. 重新部署（文章从线上消失）
hexo clean && hexo generate && hexo deploy
```

**恢复上线：**
```bash
# 将文章从 _drafts 移回 _posts
move "source\_drafts\文章名.md" "source\_posts\"

# 重新部署
hexo clean && hexo generate && hexo deploy
```

#### 方式二：直接删除（彻底移除）

```bash
# 1. 删除文章文件
del "source\_posts\文章名.md"

# 2. 重新部署
hexo clean && hexo generate && hexo deploy
```

### 3.6 操作注意事项

| 要点 | 说明 |
|------|------|
| **每次操作后都要部署** | 修改/下线后必须执行 `hexo clean && hexo generate && hexo deploy` |
| **hexo clean 必须执行** | 清除旧缓存，否则修改可能不生效 |
| **GitHub Pages 有延迟** | 部署后等 1-2 分钟才能看到变化 |
| **草稿不会被发布** | `_drafts/` 目录下的文章不会出现在博客上 |
| **信息安全** | 修改时不要添加公司代码、数据等敏感信息 |

### 3.7 创建分类页和标签页

```bash
# 创建分类页
hexo new page categories
# 编辑 source/categories/index.md，添加 type: categories

# 创建标签页
hexo new page tags
# 编辑 source/tags/index.md，添加 type: tags

# 创建关于页
hexo new page about
# 编辑 source/about/index.md，写入自我介绍
```

分类页 `source/categories/index.md` 示例：
```markdown
---
title: 分类
date: 2026-08-31
type: categories
---
```

---

## 四、发布流程

### 4.1 标准发布流程

```bash
cd ~/github/my-blog

# 1. 清除旧文件
hexo clean

# 2. 生成静态文件
hexo generate

# 3. 部署到 GitHub Pages
hexo deploy
```

### 4.2 一键发布

```bash
hexo clean && hexo generate && hexo deploy
```

### 4.3 发布前本地预览

```bash
# 启动本地服务器
hexo server

# 浏览器打开 http://localhost:4000 预览
# 确认无误后 Ctrl+C 停止，再执行部署 hexo clean && hexo generate && hexo deploy
```

### 4.4 发布后验证

部署完成后等待 1-2 分钟，访问 https://zhangshihang2.github.io 查看更新。

---

## 五、图片管理

### 5.1 文章配图

将图片放在 `source/img/` 目录下，在文章中引用：

```markdown
![图片描述](/img/example.jpg)
```

### 5.2 文章资源文件夹（推荐）

在 `_config.yml` 中开启：
```yaml
post_asset_folder: true
```

之后每次 `hexo new` 会自动创建同名文件夹，图片放在其中：
```
source/_posts/
├── my-post.md
└── my-post/
    └── image.jpg
```

文章中引用：
```markdown
{% asset_img image.jpg 图片描述 %}
```

---

## 六、常用配置速查

### 6.1 `_config.yml` 关键配置

```yaml
# 站点信息
title: ZhangShihang's Blog
author: ZhangShihang2
language: zh-CN
url: https://zhangshihang2.github.io

# 主题
theme: butterfly

# 文章永久链接格式
permalink: :year/:month/:day/:title/
# 或更简洁的格式
permalink: posts/:title/

# 文章资源文件夹
post_asset_folder: true

# 部署
deploy:
  type: git
  repo: https://github.com/ZhangShihang2/ZhangShihang2.github.io.git
  branch: main
```

### 6.2 常用 Hexo 命令

| 命令 | 说明 |
|------|------|
| `hexo new "标题"` | 新建文章 |
| `hexo new page "页面名"` | 新建页面 |
| `hexo generate` / `hexo g` | 生成静态文件 |
| `hexo server` / `hexo s` | 启动本地预览 |
| `hexo deploy` / `hexo d` | 部署到远程 |
| `hexo clean` | 清除缓存和 public |
| `hexo list` | 列出所有文章 |

---

## 七、故障排查

| 问题 | 解决方案 |
|------|---------|
| 部署后页面 404 | 等待 1-2 分钟 GitHub Pages 构建；检查仓库 Settings → Pages |
| 部署认证失败 401 | 检查 `.git-credentials` 中的 PAT 是否有效 |
| 主题不生效 | 确认 `_config.yml` 中 `theme: butterfly` |
| 中文乱码 | 确保文件以 UTF-8 编码保存 |
| 图片不显示 | 检查路径是否以 `/` 开头（如 `/img/x.jpg`） |
| 文章不出现在首页 | 检查 `date` 是否是未来时间，或是否在 `_drafts` 目录 |

---

## 八、信息安全检查清单

每次发布前确认：

- [ ] 文章内容不包含公司代码、数据、业务逻辑
- [ ] 文章不包含内网链接（pdd.net、gitlab 等）
- [ ] 文章不包含员工信息、公司邮箱
- [ ] 图片不包含公司代码、架构图、内部系统截图
- [ ] 所有内容来源于公开技术资料

> ⚠️ 禁止上传公司研发资料到个人 GitHub，严格遵守信息安全守则。
